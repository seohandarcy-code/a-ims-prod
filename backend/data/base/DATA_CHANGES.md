# raw_dt_new.dat 데이터 변경 안내

`raw_dt.dat`를 기반으로 신규 컬럼 9개를 추가한 `raw_dt_new.dat`가 생성되었습니다.
현재 vue-fastapi-nginx 기반 웹 화면은 `raw_dt.dat`(29개 컬럼) 구조를 전제로 만들어져 있으므로,
`raw_dt_new.dat`(38개 컬럼)로 데이터 소스를 교체/확장할 때 아래 내용을 참고해 주세요.

- 원본: `C:\study\raw_dt.dat` (29 컬럼, 34 행)
- 신규: `C:\study\raw_dt_new.dat` (38 컬럼, 34 행)
- 형식: TSV(tab-separated), UTF-8, CRLF — 원본과 동일

## 1. 컬럼명 변경

| 기존 | 변경 | 비고 |
|---|---|---|
| `조직` | `PJT` | 값(시스템_PJT / 인프라DT_PJT / 자동화_PJT)은 변경 없음, 컬럼명만 변경 |

프론트/백엔드에서 `조직` 필드를 참조하는 코드가 있다면 `PJT`로 이름을 맞춰야 합니다.

## 2. 컬럼 순서 및 삽입 위치

기존 컬럼 순서는 유지되며, 아래 위치에 신규 컬럼이 삽입/추가되었습니다.

```
NO → 팀(신규) → PJT(구 조직) → 파트(신규) → 담당자(변경전) → 담당자 → WBS_Code → ...
   ... (기존 24개 컬럼 순서 동일) ...
   → 기성처리금액_합계 → ERP등록(신규) → IRB심의(신규) → IT-PMS(신규)
   → 계약월_입력(신규) → 계약금액_입력(신규) → 계약완료구분(신규) → 실행품의금액(신규)
```

전체 컬럼 인덱스(0-base)는 아래 3번 표를 참고하세요.

## 3. 신규 컬럼 상세 스펙

| # | 컬럼명 | 타입 | 값 도메인 | 설명 |
|---|---|---|---|---|
| 1 | `PJT` | 조직 | `시스템_PJT` / `인프라DT_PJT` / `자동화_PJT` | 기존 `조직` 값 유지 |
| 2 | `팀` | 문자열 (상수) | `A-Infra기술팀` | 전 행 동일 값 |
| 3 | `파트` | enum (PJT별 하위) | 아래 4번 매핑표 참조 | PJT 하위 조직 |
| 4 | `ERP등록` | enum / nullable | `등록` 또는 빈값 | ERP 시스템 등록 여부 |
| 5 | `IRB심의` | enum / nullable | `완료` 또는 빈값 | IRB(투자심의) 진행 여부 |
| 6 | `IT-PMS` | enum / nullable | `완료` 또는 빈값 | IT 프로젝트관리시스템 등록 여부 |
| 7 | `계약월_입력` | 문자열 (예: `9월`) / nullable | 수기 입력용, 기존 `계약_월`과 별개 개념 | 실제 계약 체결월 |
| 8 | `계약금액_입력` | 숫자 문자열 / nullable | 수기 입력용, 기존 금액 컬럼과 별개 | 실제 계약금액 |
| 9 | `계약완료구분` | enum / nullable | `완료` 또는 빈값 | 파생 컬럼 (4번 로직 참조) |
| 10 | `실행품의금액` | 숫자 문자열 / nullable | 파생 컬럼 (4번 로직 참조) | 실행 단계 품의 금액 |

> 위 표는 10개 항목이지만 `PJT`는 이름 변경, 나머지 9개가 실제 신규 컬럼입니다.

## 4. 컬럼 간 의존관계 / 파생 로직 (목업 데이터 생성 규칙)

**중요:** 아래는 이번에 생성한 **목업 데이터 규칙**입니다. 실데이터 연동 시 동일한 업무 로직이 적용될 가능성이 높으므로, 백엔드 검증/계산 로직 설계 시 참고하세요.

### 4.1 심의완료 판정 (선행 조건)
```
심의완료 = (팀장심의_의견 == '승인')
           AND (센터장_심의필요 == '불필요' OR 센터장_심의의견 == '승인')
```
심의완료 건에 한해서만 `ERP등록` 또는 `IRB심의` 경로가 진행됩니다. (34건 중 21건 해당)

### 4.2 ERP / IRB 배타 경로
- 심의완료 건은 **ERP등록 경로** 또는 **IRB심의 경로** 중 하나만 가집니다 (동시에 둘 다 진행되지 않음).
- 목업 데이터에서는 약 3:7 비율로 배정(ERP 6건 / IRB 15건).

### 4.3 단계별 진행 (funnel 구조)
```
IRB심의 완료  →  IT-PMS 완료  →  계약월_입력 / 계약금액_입력 값 존재
```
- `IRB심의`가 값이 있어야 `IT-PMS`가 진행될 수 있음
- `IT-PMS`가 `완료`여야 `계약월_입력` / `계약금액_입력`이 채워질 수 있음
- 각 단계는 항상 다음 단계로 이어지지 않고, 일부는 빈값(미완료)으로 남을 수 있음 → 프론트/백엔드는 이 컬럼들을 **nullable**로 취급해야 함

### 4.4 계약완료구분
```
계약완료구분 = '완료'  if (ERP등록 == '등록') OR (계약월_입력 값 존재)
             else 빈값
```

### 4.5 실행품의금액
```
if ERP등록 == '등록':
    실행품의금액 = 투자비 값
elif 계약월_입력 값 존재:
    실행품의금액 = 계약금액_입력 값
else:
    실행품의금액 = 빈값
```

## 5. PJT - 파트 매핑

| PJT | 파트 (하위 조직) |
|---|---|
| `시스템_PJT` | 인프라도면관리, 인프라시스템개발, 디지털트윈솔루션, 인프라데이터플랫폼 |
| `인프라DT_PJT` | DT시뮬레이션, Vertical AI, DT시너지 |
| `자동화_PJT` | 환경안전자동화, 위험작업자동화, 시스템엔지니어링 |

행별 파트는 소속 PJT 그룹 내에서 랜덤 배정되었습니다. 화면상 PJT 선택 시 파트 드롭다운을 필터링하는 UI가 필요하다면 이 매핑을 그대로 사용하면 됩니다.

## 6. 프론트/백엔드 반영 시 체크리스트

- [ ] `조직` → `PJT` 컬럼명 변경에 따른 API 스키마/모델 필드명 업데이트
- [ ] 신규 9개 컬럼을 백엔드 데이터 모델(FastAPI Pydantic 스키마 등)에 추가
- [ ] `ERP등록` / `IRB심의` / `IT-PMS` / `계약완료구분`은 값이 있을 때만 `완료`류 배지로 표시하고, 빈값은 "미진행" 등으로 프론트에서 처리
- [ ] `계약월_입력` / `계약금액_입력` / `실행품의금액`은 숫자·월 문자열이 비어있을 수 있으므로 nullable 처리 및 숫자 포맷팅(₩ 표시 등) 필요
- [ ] `파트` 드롭다운/필터 UI 추가 시 5번 매핑표 기준으로 PJT 종속 옵션 구성
- [ ] 목업 데이터의 ERP/IRB 배타 관계, funnel 진행 로직(4번)은 실데이터 연동 전까지는 화면상 참고용이며, 실제 업무 규칙 확정 시 재검토 필요
